# Plan: Visually verify items 1–3 (Asset Modal, Company Profile, Word overflow)

## Context
Static source reads show all three items appear **already fixed** in the uncommitted working tree (pre-existing modifications from before this session). Rather than trust the static read (I was burned by a stale investigation earlier), I'll verify against the running app.

## Steps (all read-only — no code changes unless a real issue surfaces)
1. **Start the dev server** (`npm run dev`, guest mode at `/app?guest=1`).
2. **Screenshot the Asset Modal** in light + dark theme — open an asset, capture the modal. Check: compact spacing, no dark patches in the chart gridlines or fundamentals table.
3. **Screenshot the Company Profile** in light theme — navigate to a company profile. Check: finviz cells, intel tables, news items are NOT dark slabs.
4. **Screenshot the Predictions module** at a narrow width (~375px) — check the `prediction-market-title` wraps instead of overflowing.
5. **Analyze each screenshot** with the image tool for: dark-on-dark patches (leak), excessive whitespace (not compact), or horizontal overflow (broken wrap).

## Decision gate after verification
- **If all clean** → items 1–3 are confirmed done; I report that and we close them.
- **If a real issue surfaces** → I'll pause and show you the screenshot + propose a specific fix before editing anything.

## Out of scope
No code changes are planned in this step. If verification is clean, nothing gets edited. If something's broken, I'll present the fix for approval rather than edit blindly. No commits.