{
  "name": "zenin-docs-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Public VitePress documentation portal for Zenin (Get started + Use Zenin).",
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "preview": "vitepress preview .",
    "check:links": "node scripts/check-links.mjs",
    "check:frontmatter": "node scripts/check-frontmatter.mjs",
    "lint:docs": "npm run check:frontmatter && npm run check:links"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "markdownlint-cli": "^0.43.0"
  }
}
